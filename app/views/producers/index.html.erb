<!doctype html>
<html lang="en">
	<head>
		<!-- Required meta tags -->
		<meta charset="utf-8">
		<meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">

		<!-- Bootstrap CSS -->
		<link rel="stylesheet" href="node_modules/bootstrap/dist/css/bootstrap.min.css">
		<link rel="stylesheet" href="css/main.css">
		<link rel="stylesheet" href="node_modules/open-iconic/font/css/open-iconic-bootstrap.min.css">

		<title>Viveros</title>
	</head>
	<body>
  		<%= render "/partials/menu" %>

		<div class="jumbotron">	
			<h1>Gestor de productores</h1>
		</div>	
		<div class="container">

			<nav aria-label="breadcrumb">
				<ol class="btn btn-primary">
					<%= link_to "Agregar productor", "/producers/new" %>
				</ol>
            </nav>

			<div class="table-responsive">
				<table class="table table-striped">
					<thead class="thead-dark">
						<th>Nombre</th>
						<th>Apellido</th>
                        <th>Tipo de documento</th>
                        <th>Documento</th>
						<th>Fecha Cración</th>
						<th COLSPAN="2">Actualizar</th>

					</thead>
                    <tbody>
                        <% @producers.each do |producer| %>
						<tr>	
							<td><%= producer.name %></td>
                            <td><%= producer.lastname %></td>
                            <td><%= producer.document_type %></td>
                            <td><%= producer.document_number %></td>
							<td><%= producer.created_at %></td>
							<td><%= link_to "Editar", edit_producer_path(producer) %></td>
							<td><%= link_to "Eliminar", producer, method: :delete, data: { confirm: "Seguro?" } %></td>
						</tr>
						
                        <% end %>
					</tbody>
				</table>			
			</div>
		</div>
		
		<footer>
			<%= render "/partials/footer" %>
		</footer>
	

		<!-- Optional JavaScript -->
		<!-- jQuery first, then Popper.js, then Bootstrap JS -->
		<script src="node_modules/jquery/dist/jquery.min.js"></script>
		<script src="node_modules/popper.js/dist/popper.min.js"></script>
		<script src="node_modules/bootstrap/dist/js//bootstrap.min.js"></script>
	</body>
</html>